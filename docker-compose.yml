version: '3'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: neverscapealone-api:latest
    ports:
      - "5500:5500"
    env_file:
      - .env
    volumes:
      - /home/ubuntu/match_histories:/code/histories:rw
